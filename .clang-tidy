# .clang-tidy - configuration for ft_irc (C++98 compatible)

---
# Checks define quais verificações o clang-tidy vai executar
Checks: >
  clang-analyzer-*,       # Detecta bugs comuns: null pointers, memory leaks, buffer overflows
  bugprone-*,             # Detecta padrões propensos a erros típicos de C++
  performance-*,          # Sugestões para melhorar performance do código (ex.: cópias desnecessárias)
  readability-*,          # Alertas sobre estilo e clareza do código
  portability-*,          # Verifica uso de construções não portáveis entre plataformas
  cppcoreguidelines-*,    # Boas práticas gerais de C++ (compatíveis com C++98)
  misc-*                  # Verificações diversas úteis

# HeaderFilterRegex define quais arquivos de cabeçalho serão verificados
HeaderFilterRegex: 'includes/.*'  # Só verifica os headers dentro da pasta includes/

# CheckOptions permite configurar opções específicas de algumas verificações
CheckOptions:
  - key: readability-magic-numbers.IgnoredValues
    value: '0,1'                 # Ignora alertas de “magic numbers” para valores comuns usados em IRC flags (0 e 1)
